name: Upstream Sync

on:
  schedule:
    - cron: "0 0 * * *" # Daily at 0:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: linux-wsl2-build
          fetch-depth: 0
          token: ${{ secrets.PAT }}

      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Add Upstream and Fetch
        run: |
          git remote add upstream https://github.com/Roblox/studio-rust-mcp-server.git
          git fetch upstream

      - name: Merge Upstream Changes
        run: |
          echo "Merging upstream/main into linux-wsl2-build..."

          # Execute merge without auto-commit
          set +e # Do not stop on error
          git merge upstream/main --no-commit --no-ff
          MERGE_EXIT_CODE=$?
          set -e

          # ALWAYS restore our versions of README.md and the .github directory
          # Unstage any changes to these paths
          git reset HEAD .github README.md || true

          # Checkout our versions
          git checkout HEAD -- .github README.md || true

          # Clean any new files upstream might have added to .github
          git clean -fd .github || true

          if [ $MERGE_EXIT_CODE -ne 0 ]; then
             echo "Merge conflict detected. resolving..."
             
             # Mark our restored versions as resolved
             git add .github README.md || true
             
             # Check if any other conflicts remain
             if git diff --name-only --diff-filter=U | grep .; then
               echo "Unresolved conflicts in other files."
               exit 1
             fi
             
             git commit -m "Merge upstream/main into linux-wsl2-build (Auto-resolved conflicts)"
          else
             # No conflicts or auto-resolved
             echo "Merge successful (or no conflicts)."
             # Commit if stopped by no-commit
             git commit -m "Merge upstream/main into linux-wsl2-build" || echo "Nothing to commit"
          fi

      - name: Push Changes
        run: git push origin linux-wsl2-build
